
{
    "builds":[{
        "src": "tribe/wsgi.py",
        "use": "@vercel/python",
        "config": {"maxLambdaSize": "100mb", "runtime": "python 3.9"}
        }],
    "routes": [
        { 
          "src": "/(.*)",
          "dest": "tribe/wsgi.py"
        }
    ]
}