{
    "builds": [{
        "src": "tribe/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "200mb", "runtime": "python3.12" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "tribe/wsgi.py"
        }
    ]
}